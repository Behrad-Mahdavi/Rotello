# پرامپت ساخت پروژه: سیستم مدیریت تسک باشگاه کسب‌وکار رکاد (نسخه‌ی ساده‌شده‌ی Trello)

> این فایل یک بریف کامل فنی/منطقی است. لطفاً کل فایل را قبل از شروع کدنویسی بخوان، و در صورت هر ابهامی به‌جای فرض کردن، صریحاً بپرس.

---

## ۱. معرفی پروژه و استک فنی

یک اپلیکیشن مدیریت پروژه/تسک شبیه Trello اما بسیار ساده‌تر، برای باشگاه کسب‌وکار مدرسه‌ی استارتاپی رکاد. دانش‌آموزها (عضو) روی تسک‌هایی که مدیر باشگاه بهشون می‌ده کار می‌کنن، پیشرفتشون رو روی یک بورد کانبان جابه‌جا می‌کنن.

**استک فنی:**
- Frontend: Next.js (App Router) + TypeScript + Tailwind
- Backend/DB/Auth: Supabase (Postgres + Auth + RLS)
- Deploy: Vercel
- Drag & Drop برای بورد کانبان: هر کتابخونه‌ی سبک مثل `@dnd-kit/core`

---

## ۲. نقش‌ها (Roles)

فقط دو نقش وجود دارد که در ستون `role` روی جدول `profiles` ذخیره می‌شود:

- `admin` — مدیر باشگاه
- `member` — عضو عادی (دانش‌آموز)

هیچ نقش سومی، هیچ سلسله‌مراتب دیگری، و **هیچ لیدربورد/رقابتی وجود ندارد.**

### ورود اعضا
- احراز هویت با **Supabase Auth** (ایمیل + پسورد).
- مدیر از تب «مدیریت اعضا» مستقیماً حساب عضو را با ایمیل و پسورد اولیه می‌سازد (از طریق Supabase Admin API / service role، سمت سرور — نه از کلاینت).
- عضو با همان ایمیل/پسورد وارد می‌شود؛ امکان تغییر پسورد شخصی بعداً اختیاری است.
- ثبت‌نام آزاد (self sign-up) وجود ندارد. فقط مدیر می‌تواند کاربر جدید بسازد.

---

## ۳. مدل داده (Supabase / Postgres)

### 3.1 `profiles`
گسترش `auth.users`:

| ستون | نوع | توضیح |
|---|---|---|
| id | uuid (PK, FK → auth.users.id) | |
| full_name | text | |
| role | text check in ('admin','member') | |
| xp_total | integer, default 0 | جمع کل XP کسب‌شده |
| created_at | timestamptz, default now() | |

### 3.2 `projects`
| ستون | نوع | توضیح |
|---|---|---|
| id | uuid PK default gen_random_uuid() | |
| name | text not null | |
| description | text | |
| created_by | uuid FK → profiles.id | باید admin باشد |
| created_at | timestamptz default now() | |

### 3.3 `tasks`
| ستون | نوع | توضیح |
|---|---|---|
| id | uuid PK | |
| project_id | uuid FK → projects.id, not null | |
| title | text not null | |
| description | text | |
| status | text check in ('backlog','todo','in_progress','done'), default 'backlog' | |
| xp_value | integer not null default 0 | امتیازی که این تسک ارزش دارد |
| created_by | uuid FK → profiles.id | |
| created_at | timestamptz default now() | |
| updated_at | timestamptz default now() | هر بار status عوض شد آپدیت شود (trigger) |

### 3.4 `task_assignees` (رابطه چند-به-چند)
| ستون | نوع |
|---|---|
| task_id | uuid FK → tasks.id |
| user_id | uuid FK → profiles.id |
| assigned_at | timestamptz default now() |

PK ترکیبی: (task_id, user_id)

### 3.5 `checklists`
| ستون | نوع |
|---|---|
| id | uuid PK |
| task_id | uuid FK → tasks.id |
| title | text not null |
| sort_order | integer default 0 |

هر تسک می‌تواند چندین رکورد checklist داشته باشد.

### 3.6 `checklist_items`
| ستون | نوع |
|---|---|
| id | uuid PK |
| checklist_id | uuid FK → checklists.id |
| content | text not null |
| is_done | boolean default false |
| done_by | uuid FK → profiles.id, nullable |
| done_at | timestamptz, nullable |
| sort_order | integer default 0 |

### 3.7 `task_reports` (لاگ گزارش‌ها — append-only)
| ستون | نوع |
|---|---|
| id | uuid PK |
| task_id | uuid FK → tasks.id |
| author_id | uuid FK → profiles.id |
| content | text not null |
| created_at | timestamptz default now() |

**هیچ‌گاه UPDATE/DELETE روی این جدول مجاز نیست** — فقط INSERT و SELECT. این یک لاگ تاریخ‌دار از گزارش‌های متوالی عضو است (مثل کامنت)، نه یک فیلد قابل‌بازنویسی.

---

## ۴. منطق کسب‌وکار (این بخش حیاتی‌ست — منبع اصلی جلوگیری از باگ منطقی)

### 4.1 ترتیب مراحل بورد کانبان
```
backlog → todo → in_progress → done
```
ثابت و همیشه به همین ترتیب. هیچ مرحله‌ی موازی یا شاخه‌ای وجود ندارد.

### 4.2 قوانین جابه‌جایی مرحله (این باید حتماً در یک تابع سمت سرور/RPC پیاده شود، نه فقط RLS ساده روی UPDATE)
بساز یک Postgres function به اسم `move_task_status(p_task_id uuid, p_new_status text)` که:

1. کاربر فعلی را از `auth.uid()` می‌گیرد.
2. اگر کاربر `admin` است → اجازه دارد تسک را به **هر** مرحله‌ای ببرد (جلو یا عقب)، از جمله `done`.
3. اگر کاربر `member` است:
   - فقط اگر در `task_assignees` برای این تسک ثبت شده باشد (یعنی مسئول این تسک است) اجازه دارد.
   - فقط اجازه دارد **یک پله به جلو** برود (backlog→todo، todo→in_progress، in_progress→done ممنوع!).
   - **member هرگز نمی‌تواند تسک را به `done` ببرد** — این پله مخصوص admin است. اگر member تلاش کند تسک را از `in_progress` جلو ببرد، باید خطا برگردد (چون مقصد `done` است و فقط admin مجاز است).
   - member نمی‌تواند به عقب برگرداند (فقط رو به جلو).
4. اگر هیچ‌کدام از شرایط بالا برقرار نبود → exception پرتاب شود (`raise exception`), و کلاینت باید پیام خطای مناسب نمایش دهد.
5. در صورت موفقیت: `status` و `updated_at` آپدیت شوند.

> چرا RPC و نه RLS ساده؟ چون منطق «فقط یک پله جلو» و «مقصد done قدغن برای member» نیاز به مقایسه‌ی status قدیم/جدید دارد که در policy های RLS معمولی (`USING`/`WITH CHECK`) به‌سختی و با ریسک خطای race condition پیاده می‌شود. تابع تراکنشی امن‌تر و قابل تست‌تر است.

### 4.3 XP
- `xp_value` روی خود تسک، در لحظه‌ی ساخت تسک توسط admin تعیین می‌شود (زیر فیلد انتخاب مسئول، همان مودال ساخت تسک).
- XP واقعاً به `profiles.xp_total` هر assignee **فقط وقتی تسک به `done` می‌رسد** اضافه می‌شود (چون فقط admin آن را done می‌کند، این معادل تایید نهایی کار است). این را داخل همان تابع `move_task_status` هندل کن: وقتی p_new_status = 'done' شد، به ازای هر ردیف در `task_assignees` مربوط به آن تسک، `xp_value` را به `xp_total` آن کاربر اضافه کن.
- اگر تسک چند مسئول دارد، **هرکدام کامل `xp_value` را می‌گیرند** (نه تقسیم‌شده) — چون XP معیار رقابتی نیست، فقط ثبت تاریخچه‌ی شخصی است.
- **مهم:** باید جلوی double-counting گرفته شود — اگر تسک یک‌بار done شد و admin دوباره status را تغییر داد و برگرداند و دوباره done کرد، نباید XP دوباره اضافه شود. راه‌حل ساده: یک ستون boolean `xp_awarded` روی `tasks` اضافه کن؛ فقط وقتی `xp_awarded = false` بود و مقصد done شد، XP بده و `xp_awarded = true` کن.
- XP فقط در پروفایل شخص خودش نمایش داده می‌شود (`xp_total`). **هیچ صفحه‌ی مقایسه‌ای/لیدربورد وجود ندارد.**

### 4.4 چک‌لیست
- هر تسک می‌تواند چند checklist داشته باشد؛ هر checklist چند آیتم.
- هر assignee (و admin) می‌تواند آیتم‌های چک‌لیست را تیک بزند/بردارد (`is_done`, `done_by`, `done_at`).
- تیک زدن چک‌لیست، به‌خودی‌خود status تسک را جابه‌جا **نمی‌کند** (این دو مستقل‌اند مگر خلاف آن گفته شود).
- فقط admin می‌تواند checklist/آیتم جدید بسازد یا حذف کند؛ member فقط تیک می‌زند.

### 4.5 گزارش‌ها (Reports)
- عضو در صفحه‌ی جزئیات تسک **فقط** می‌تواند یک گزارش جدید در `task_reports` اضافه کند (INSERT). هیچ فیلد دیگری از تسک (title, description, checklist items, assignees, status) از این مسیر قابل ویرایش نیست.
- گزارش‌ها به‌ترتیب تاریخ نمایش داده می‌شوند (مثل تایم‌لاین/کامنت). قابل ویرایش یا حذف نیستند.
- admin هم می‌تواند گزارش‌ها را ببیند (فقط خواندن؛ اگر لازم شد بعداً می‌تواند نظر بدهد — فعلاً خارج از اسکوپ).

### 4.6 ساخت پروژه/تسک
- فقط admin: می‌تواند پروژه بسازد.
- فقط admin: می‌تواند تسک بسازد، از طریق یک مودال با فیلدهای زیر (همگی الزامی مگر خلاف ذکر شود):
  1. عنوان تسک
  2. توضیحات تسک
  3. یک یا چند چک‌لیست (هرکدام قابل افزودن چند آیتم)
  4. یک یا چند مسئول (چند-انتخابی از لیست اعضا)
  5. پروژه‌ی مرتبط (اگر داخل صفحه‌ی یک پروژه باز شده، از قبل پیش‌فرض همان پروژه است)
  6. مقدار XP این تسک (عدد)
- تسک تازه‌ساخته‌شده همیشه با `status = 'backlog'` شروع می‌شود.

---

## ۵. Row Level Security — راهنمای کلی

فعال‌سازی RLS روی همه‌ی جدول‌ها الزامی است. خلاصه‌ی سیاست‌ها:

| جدول | SELECT | INSERT/UPDATE/DELETE |
|---|---|---|
| profiles | همه‌ی کاربران احرازشده می‌توانند همه‌ی پروفایل‌ها را بخوانند (برای نمایش نام‌ها) | فقط admin (یا service role از سرور) می‌تواند رکورد جدید بسازد یا role تغییر دهد؛ خود کاربر می‌تواند فقط full_name خودش را ادیت کند |
| projects | همه‌ی کاربران احرازشده | فقط admin |
| tasks | همه‌ی کاربران احرازشده (member همه‌ی تسک‌های پروژه را می‌بیند، نه فقط تسک‌های خودش) | ساخت/حذف/ادیت فیلدهای متنی: فقط admin. تغییر status: فقط از طریق تابع `move_task_status` (اجرا با privilege تابع، نه UPDATE مستقیم روی جدول) |
| task_assignees | همه | فقط admin |
| checklists / checklist_items | همه | ساخت/حذف: فقط admin. توگل `is_done`: admin یا کاربری که در `task_assignees` همان تسک است |
| task_reports | admin و assigneeهای همان تسک | INSERT فقط توسط admin یا assigneeهای همان تسک؛ UPDATE/DELETE برای همه ممنوع |

> نکته‌ی امنیتی: مستقیماً به کلاینت اجازه‌ی UPDATE ستون `status` روی `tasks` نده؛ این ستون باید فقط از طریق RPC (`move_task_status`) قابل تغییر باشد تا منطق بخش ۴.۲ نقض نشود.

---

## ۶. صفحات / مسیرها

### مدیر (admin)
- `/admin/members` — لیست اعضا، ساخت عضو جدید (ایمیل+پسورد)، مشاهده‌ی XP هرکس (اختیاری اینجا هم قابل دیدن admin است، چون خودش دسترسی کامل دارد؛ این با «بدون لیدربورد برای اعضا» تناقضی ندارد)
- `/admin/projects` — لیست/ساخت/ویرایش/حذف پروژه‌ها
- `/projects/[projectId]/board` — بورد کانبان کامل پروژه؛ admin می‌تواند تسک بسازد، هر تسکی را به هر مرحله‌ای (شامل done) ببرد

### عضو (member)
- `/projects/[projectId]/board` — همان بورد کانبان، اما:
  - همه‌ی تسک‌های پروژه دیده می‌شود (نه فقط مال خودش)
  - فقط تسک‌هایی که خودش assignee آن‌هاست قابل درگ‌کردن هستند، و فقط یک ستون به جلو، و هرگز به done
  - کلیک روی هر تسک (چه مال خودش چه دیگران) جزئیات را باز می‌کند
- `/projects/[projectId]/tasks/[taskId]` (یا مودال) — جزئیات تسک: عنوان، توضیحات (فقط نمایش)، چک‌لیست‌ها (قابل تیک‌زدن اگر assignee باشد)، لیست مسئولین، بخش گزارش‌ها (تایم‌لاین + فرم افزودن گزارش جدید)
- `/profile` — نمایش نام و `xp_total` شخص

هدایت بین این دو ست صفحه باید بر اساس `profiles.role` در middleware/layout انجام شود (admin به تب‌های اضافه‌ی مدیریتی دسترسی دارد، member فقط بورد و پروفایل خودش را می‌بیند).

---

## ۷. جریان‌های کاربری کلیدی (User Flows)

**ساخت تسک توسط admin:**
1. admin وارد `/projects/[id]/board` می‌شود → دکمه‌ی «تسک جدید»
2. مودال باز می‌شود: عنوان، توضیحات، افزودن یک یا چند checklist با آیتم‌هاشون، انتخاب مسئول(ها) از بین اعضا، مقدار XP
3. ثبت → ردیف در `tasks` با status='backlog' + ردیف(های) در `task_assignees` + ردیف‌های `checklists`/`checklist_items` ساخته می‌شود

**پیشرفت تسک توسط member:**
1. عضو تسک خودش را در ستون backlog می‌بیند → درگ به todo (یا دکمه‌ی «مرحله بعد»)
2. کلاینت `move_task_status(taskId, 'todo')` را صدا می‌زند
3. تابع چک می‌کند: کاربر assignee است؟ مقصد فقط یک پله جلوتر است؟ ✅ → آپدیت انجام می‌شود
4. وقتی تسک در `in_progress` است، عضو نمی‌تواند خودش آن را done کند؛ فقط admin از بورد خودش این کار را می‌کند و در همان لحظه XP به `xp_total` assigneeها اضافه می‌شود

**ثبت گزارش توسط عضو:**
1. عضو روی تسک خودش کلیک می‌کند → جزئیات باز می‌شود (بدون امکان ادیت عنوان/توضیح/چک‌لیست ساختاری)
2. پایین صفحه فرم «افزودن گزارش» → متن می‌نویسد → ثبت → ردیف جدید در `task_reports` (گزارش‌های قبلی هم‌چنان بالای آن دیده می‌شوند، بدون امکان ویرایش)

---

## ۸. خارج از اسکوپ (فعلاً پیاده‌سازی نشود)

- لیدربورد یا هر نوع مقایسه‌ی XP بین اعضا
- نقش‌های بیشتر از admin/member
- کامنت‌گذاری admin روی گزارش‌ها
- نوتیفیکیشن/ایمیل خودکار
- تسک‌های زیرمجموعه (subtask) یا وابستگی بین تسک‌ها
- تاریخ سررسید (due date) — مگر بعداً خواسته شود

---

## ۹. چک‌لیست پذیرش (Acceptance Criteria) — برای تست نهایی

- [ ] member نمی‌تواند پروژه یا تسک بسازد (نه از UI، نه با درخواست مستقیم به API/RLS)
- [ ] member نمی‌تواند تسکی را که assignee آن نیست جابه‌جا کند
- [ ] member نمی‌تواند هیچ تسکی را مستقیماً به `done` ببرد، حتی تسک خودش را
- [ ] member نمی‌تواند بیش از یک پله در یک حرکت جلو برود
- [ ] admin می‌تواند هر تسکی را به هر مرحله‌ای (از جمله done) ببرد
- [ ] وقتی admin تسکی را done می‌کند، XP فقط **یک‌بار** به هر assignee اضافه می‌شود، حتی اگر status بعداً برگردد و دوباره done شود
- [ ] عضو در صفحه‌ی جزئیات تسک هیچ فیلدی به‌جز چک‌لیست (تیک) و افزودن گزارش جدید را نمی‌تواند تغییر دهد
- [ ] گزارش‌های ثبت‌شده هرگز قابل ویرایش یا حذف نیستند (نه از UI و نه از RLS)
- [ ] هیچ صفحه یا API‌ای مقایسه‌ی XP بین کاربران را نشان نمی‌دهد
- [ ] عضو تمام تسک‌های پروژه را در بورد می‌بیند، نه فقط تسک‌های خودش
